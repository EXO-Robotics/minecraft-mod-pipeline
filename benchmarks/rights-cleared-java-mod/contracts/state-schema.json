{
  "schema_version": "1.0.0",
  "contract_id": "doorlock-bedrock-state-v1",
  "status": "IMPLEMENTED_PROPOSED_REDESIGN_RUNTIME_NOT_VERIFIED",
  "storage_scope": "world",
  "logical_schema": {
    "schema_version": {"type": "integer", "const": 1},
    "locks": {
      "type": "map",
      "key_format": "<dimension>:<x>:<y>:<z>",
      "value": {
        "schema": {"type": "integer", "const": 1},
        "dimension": "string",
        "position": {"x": "integer", "y": "integer", "z": "integer"},
        "authorization": {
          "one_of": [
            {"authorization_mode": "owner_identity", "owner": "stable_player_id"},
            {"authorization_mode": "legacy_credential", "owner": "legacy-unclaimed", "credential_digest": "nonempty_string"}
          ]
        },
        "created_by_player_id": "stable_player_id",
        "created_at_tick": "nonnegative_integer",
        "revision": "positive_integer"
      }
    }
  },
  "source_evidence": [
    {"path": "src/main/java/net/vassbo/door_lock/data/ModData.java", "lines": [6, 8], "claim": "Java source stores a global list of locked-block strings."},
    {"path": "src/main/java/net/vassbo/door_lock/util/LockData.java", "lines": [5, 8], "claim": "Each Java lock has a block position and key hash."},
    {"path": "src/main/java/net/vassbo/door_lock/util/LockCheck.java", "lines": [120, 145], "claim": "Java serialization combines coordinates and key hash."}
  ],
  "redesign_decisions": [
    "Dimension is added to prevent cross-dimension coordinate collisions.",
    "Creator identity and revision are added for multiplayer authorization and conflict control.",
    "Structured records replace delimiter-encoded strings.",
    "The active unapproved redesign uses stable player identity instead of a configured credential.",
    "Migrated legacy records preserve opaque credential digests but remain unclaimed; plaintext secrets are prohibited."
  ],
  "compatibility": {
    "sparse_owner_v1": "deterministically normalized to the complete owner_identity record before validation and persisted during startup",
    "invalid_or_unknown_records": "fail closed and keep supported block interactions disabled",
    "identifier_reuse": "prohibited"
  },
  "evidence": [
    "reconstruction/custom/scripts/doorlock-state.js",
    "tests/test_benchmark_b_reconstruction.py::BenchmarkBReconstructionTests.test_state_records_and_revision_checks_match_the_v1_contract"
  ],
  "unverified": [
    "dynamic-property records written by a real player action",
    "record recovery after player reconnect",
    "credential-form redesign approval and implementation",
    "world upgrade and rollback on Bedrock clients"
  ]
}
