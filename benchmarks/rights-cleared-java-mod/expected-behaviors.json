{
  "schema_version": "1.0.0",
  "benchmark_id": "benchmark-b-doorlock-technical-reconstruction",
  "source_revision": "fe097cf9376242eb13d53dc485b61d8b33891392",
  "status": "CONTRACT_DEFINED_NOT_RUN",
  "runtime_status": "NOT_RUN",
  "behaviors": [
    {
      "id": "doorlock:key_configure",
      "trigger": "controller_form_submit",
      "expected": ["validate_nonempty_secret", "store_key_credential_digest", "confirm_without_echoing_secret"],
      "reconstruction": "CONTROLLER_FIRST_REDESIGN",
      "evidence": [
        {"kind": "source", "path": "src/main/java/net/vassbo/door_lock/command/UpdateKey.java", "lines": [15, 29], "claim": "Java command assigns password data to a held key."}
      ],
      "verification": "NOT_RUN"
    },
    {
      "id": "doorlock:lock_block",
      "trigger": "use_key_on_unlocked_supported_block",
      "expected": ["normalize_door_to_bottom_half", "bind_block_location_to_key_digest", "persist_world_lock", "deny_same_interaction_that_created_lock"],
      "reconstruction": "BEDROCK_EVENT_AND_STATE_REDESIGN",
      "evidence": [
        {"kind": "source", "path": "src/main/java/net/vassbo/door_lock/util/LockCheck.java", "lines": [64, 80], "claim": "An unlocked block used with a configured key receives lock data and does not open."},
        {"kind": "source", "path": "src/main/java/net/vassbo/door_lock/item/KeyItem.java", "lines": [50, 84], "claim": "Key use normalizes a door position and supports lock removal while sneaking."}
      ],
      "verification": "NOT_RUN"
    },
    {
      "id": "doorlock:authorize_open",
      "trigger": "interact_with_locked_supported_block",
      "expected": ["deny_without_key", "deny_unconfigured_key", "deny_wrong_key", "allow_matching_key", "allow_universal_key_if_enabled", "show_controller_safe_feedback"],
      "reconstruction": "BEDROCK_EVENT_AND_STATE_REDESIGN",
      "evidence": [
        {"kind": "source", "path": "src/main/java/net/vassbo/door_lock/util/LockCheck.java", "lines": [26, 61], "claim": "Open authorization distinguishes missing, universal, unconfigured, incorrect, and matching keys."}
      ],
      "verification": "NOT_RUN"
    },
    {
      "id": "doorlock:remove_lock",
      "trigger": "sneak_use_matching_or_universal_key",
      "expected": ["authorize_key", "remove_exact_world_lock", "persist_removal", "confirm_removal"],
      "reconstruction": "BEDROCK_EVENT_AND_STATE_REDESIGN",
      "evidence": [
        {"kind": "source", "path": "src/main/java/net/vassbo/door_lock/item/KeyItem.java", "lines": [53, 80], "claim": "Sneak-use removes a lock for a universal or matching key."}
      ],
      "verification": "NOT_RUN"
    },
    {
      "id": "doorlock:break_locked_block",
      "trigger": "attempt_break_locked_block",
      "expected": ["apply_configured_break_policy", "never_leave_orphan_lock_state_after_successful_break"],
      "reconstruction": "BEDROCK_BREAK_EVENT_REDESIGN",
      "evidence": [
        {"kind": "source", "path": "src/main/java/net/vassbo/door_lock/event/BlockBreakEvent.java", "lines": [13, 25], "claim": "A pre-break callback checks lock state and removes it when breaking succeeds."}
      ],
      "verification": "NOT_RUN"
    }
  ],
  "global_invariants": [
    "No plaintext key secret is stored in world lock records or emitted in logs.",
    "Every expected behavior remains unverified until bound runtime evidence passes.",
    "No behavior contract implies rights clearance or Marketplace eligibility."
  ]
}
