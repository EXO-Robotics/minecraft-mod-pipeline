interface:
  display_name: "Launch Clean-room Worker"
  short_description: "Start isolated production workers safely"
  default_prompt: "Use $launch-cleanroom-production-worker to initialize an isolated Codex production worker with minimum authentication access."
