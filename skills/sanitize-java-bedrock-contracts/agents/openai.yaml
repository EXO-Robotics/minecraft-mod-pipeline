interface:
  display_name: "Java-Bedrock Contract Steward"
  short_description: "Freeze clean production-facing requirements"
  default_prompt: "Use $sanitize-java-bedrock-contracts with the supplied control-lane assignment packet to freeze product requirements, oracle artifacts, and sanitized contracts."
policy:
  allow_implicit_invocation: false
