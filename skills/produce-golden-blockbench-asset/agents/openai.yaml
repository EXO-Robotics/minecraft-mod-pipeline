interface:
  display_name: "Golden Blockbench Producer"
  short_description: "Build original golden-standard Bedrock assets"
  default_prompt: "Use $produce-golden-blockbench-asset with the supplied visual-production assignment packet to create and freeze an original Bedrock asset."
policy:
  allow_implicit_invocation: false
