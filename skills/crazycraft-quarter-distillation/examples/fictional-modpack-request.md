Use `$crazycraft-quarter-distillation` on
`tests/fixtures/fictional_large_modpack`. Generate a deterministic 25%-effort
scope, verify progression and prerequisites, explain rights and console blockers,
and keep deterministic scores unchanged during review.
