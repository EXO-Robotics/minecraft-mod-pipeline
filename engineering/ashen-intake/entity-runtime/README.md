# Packet 002 Ashen entity runtime shells

This lane binds the ten approved Ashen creature identities to exact native pass-2 geometry and animation exports plus exact native-project texture bytes. It authors dedicated BP entities, RP client entities, animation controllers, render controllers, and nine natural spawn rules.

Ash Drake is an arena-only runtime shell: it is not naturally spawnable and has no spawn-rule file. This lane contains no Kiln Sky session, completion, persistence, seal, or reward semantics. Loot components are intentionally absent for the separate economy owner.

Ecology is Ashen-specific and console-bounded: only Overworld mountain/mesa filters, groups of one or two, per-type surface density at most two, and weights at most six. No Whisperwood spawn values are transferred.

Run:

```sh
python3 engineering/ashen-intake/entity-runtime/build_ashen_entity_runtime.py
python3 -m unittest engineering/ashen-intake/entity-runtime/test_ashen_entity_runtime.py -v
```

Static parsing and reference closure do not prove Bedrock client playback, gameplay feel, BDS loading, multiplayer, console performance, PS4, Marketplace, or release readiness.
