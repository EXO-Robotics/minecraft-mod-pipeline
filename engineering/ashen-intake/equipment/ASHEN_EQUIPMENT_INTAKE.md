# Ashen Packet 006 equipment intake

Status: `DETERMINISTIC_INTAKE_COMPLETE_NATIVE_REPAIR_IDENTIFIED_ACQUISITION_REWARD_BLOCKED`

This intake is bound to integration commit `c7497dece77571f55de4f2b42219420b451627f3` and tree `1be5fb0093da6279f6267718bb7b03a88e9eea9d`. It inventories only the 14 Ashen-facing Packet 006 identities: three weapons, four armor pieces, three tools, two accessories, and two trophies.

The machine-readable authority is `ASHEN_EQUIPMENT_INTAKE.json`. It binds the exact brief, editable model, editable texture, static geometry export, static animation export, and export texture SHA-256 for every identity.

## Safe work versus blocked work

The 13 identities other than Briar Ring may receive namespace-safe item or trophy shells, target-path preparation, localization, and presentation binding without inventing acquisition or rewards. All 14 may receive native asset repair: real `effect` locator elements, portable texture bindings, `aionbound` namespace normalization, role-specific clip authoring, native reopen/save/reopen, native codec export, equivalence comparison, and fixed proof capture.

Acquisition and rewards remain fail-closed:

- `W1-001-AH` blocks non-warehouse ingredient dispositions and `drake_scale` closure.
- `W1-003-KILN-SKY` blocks Ash Drake arena behavior, ownership, reset, persistence, and boss-source horn fulfillment.
- `W1-004-AH` blocks loot bands, trophy sources, first-clear entitlements, repeat-clear rules, and recovery semantics.
- Deferred `W1-CREATIVE-005` blocks only an Ashen-tempered Briar Ring sidegrade. It does not authorize a second Briar Ring or mutation of the existing Whisperwood base.

## Native readiness

Static packet JSON and PNG inventory is valid, but none of the 14 assets is native-round-trip proven. Every editable source lacks a true locator element even though the briefs declare `effect` and the static geometry exports contain it. Every editable texture path is absolute. Source geometry and clips still use `aionforge_eq`. Packet geometry and PNGs are 32x32 while briefs declare 64x64 or a 32-64 range. Declared role clips are absent; the source exports contain only generic `idle` and `action` names.

The approved 32x32 pixels must be preserved during intake. The resolution mismatch is not permission to upscale or redraw.

## Collision and reuse boundary

The 13 new Ashen runtime identities are absent from the integration base. Existing G7 item, durability, attachable, armor, accessory-state, placeable-trophy, localization, and registry patterns are reusable as engineering framework only. `basalt_maul` overlaps the function of `basalt_hammer`, and `gale_repeater` overlaps `ash_repeater`; neither legacy identity may be silently aliased or overwritten.

`aionbound:briar_ring` already exists as the Whisperwood base item, recipe, attachable, geometry, atlas/catalog identity, and behavior. Its classification is `KEEP_EXISTING_BASE_REUSE_ONLY`. No duplicate shell or sidegrade is authorized.

## Proof boundary

This receipt proves static parse, exact scope, exact source hashes, evidence-derived native gaps, and collision/reuse classification. It does not prove Blockbench round-trip, native export equivalence, runtime pack binding, acquisition, boss rewards, Bedrock rendering, BDS, multiplayer, controller behavior, physical PS4, or Marketplace readiness.
