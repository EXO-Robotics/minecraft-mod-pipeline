# Packet 006 missing-asset native gate

This isolated, native-only lane is bound to G8 commit
`edbdf01143e994cae8e77414951d07ae3c95ed63` and tree
`9685cd17539999419d3f8e32272261e585cde0c6`.

It proves Blockbench 5.1.6 open/save/reopen and two native export cycles for
`surveyor_medallion`, `surveyor_staff`, `trail_compass`, and `warden_sigil`.
The authored clip sets exactly match the packet briefs: no clip for the static
medallion, `hold`, `needle_idle`, and `pulse`, respectively. Each exact
`effect` locator is a true native locator. Geometry/UV signatures and texture
bytes remain preserved.

This lane adds no BP/RP binding, item, recipe, runtime, gameplay, progression,
authority, or BDS claim.
