# Whisperwood Plant-Class Native Gate

Status: `PARTIAL_PASS_ANIMATION_REPAIR_REQUIRED`.

Six assets pass the same native Blockbench 5.1.6 save/close/reopen/export gate
used by the block class:

- `briar_vine`
- `ember_thistle`
- `glow_moss`
- `hollow_lily`
- `mooncap_mushroom`
- `root_flower`

Their committed receipts bind copied input hashes, the native `effect` locator
repair, two native exports, warning count, and proof boundary.

Four assets were withheld before editor mutation because their briefs require a
role clip that the packet export does not contain:

| Asset | Required clip |
| --- | --- |
| `lantern_bloom` | `glow_idle` |
| `pale_reed` | `sway` |
| `star_grass` | `wind_sway` |
| `whisper_fern` | `gentle_sway` |

Engineering may author those exact approved motion identities, but the native
gate will not substitute the packet's generic `idle`/`action` previews or
invent additional clips. Until repaired and rerun, the four assets are not
eligible for shipping promotion.

This evidence is limited to native editable and codec behavior. It does not
prove Bedrock client rendering, BDS, world generation, harvesting, console, or
shipping readiness.
