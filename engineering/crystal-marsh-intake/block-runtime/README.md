# Crystal Marsh ordinary full-cube block foundation

Status: static BP/RP source complete for the ten Packet 003 block warehouse
IDs. The exact base is G8 commit
`466a061cbe22a01a4e561169df31e4f351edea71`, tree
`1aadcfab635991f6d0fb4647f6ed2a3bb615a7af`.

The deterministic author binds exact `aionbound` IDs, ordinary full-cube BP
definitions, byte-identical Packet 003 textures, terrain and block registries,
and English localization. Packet briefs, editable models, geometry exports,
animations, and source textures are hash-bound as intake evidence.

Blockbench is `NOT_APPLICABLE`: these shipping forms are ordinary full cubes.
The packet's generic custom geometry and animations are not promoted. One
opaque material pipeline is used because all ten exact packet PNGs are fully
opaque.

```sh
python3 engineering/crystal-marsh-intake/block-runtime/author_crystal_blocks.py
python3 engineering/crystal-marsh-intake/block-runtime/test_crystal_blocks.py -v
```

This slice does not implement or prove acquisition, loot, recipes, worldgen,
scripts, persistence, client rendering, BDS, packaging, console, or release.
