# Wave 1 final authority closure map

Status: `FINAL_AUTHORITY_CLOSURE_MAP_ACTION_REQUIRED`

Audited integration source: commit `edbdf01143e994cae8e77414951d07ae3c95ed63`, tree `9685cd17539999419d3f8e32272261e585cde0c6`.

This is a product-isolated authority and deferred-ticket audit. It changes no product, authority, build, BDS, G7, reviewer, or release state.

## Outcome

The full Wave 1 candidate is not yet authority-closed. Five surfaces remain final-blocking:

1. Activate the already-authorized Ashen equipment and Kiln Sky services in the existing shared handlers.
2. Ratify and execute Skyreach `W1-001-SR`, a refined `W1-003-STORM-NEST`, and a refined `W1-004-SR`.
3. Resolve and implement the Twinbond finale under `W1-CREATIVE-002/003/004`, superseding the incompatible inherited G7 reward path.
4. Close `W1-ASSET-AUDIO-001` with original provenance-backed audio or an explicit final-contract scope reduction.
5. After product closure, create the deterministic immutable package and run exact-package entrypoint, Stable BDS, restart, cleanup, and evidence-derived qualification.

`W1-CREATIVE-005` is safe to leave deferred only if every named sibling/unique finish remains absent, approved base Packet 006 items remain, no substitute identity is invented, and the omission is stated as a known limitation.

## Current Skyreach evidence

The earlier partial snapshots are superseded. Exact HEAD now contains all ten creatures, all ten plants, and native PASS evidence for all thirty custom Skyreach creature, plant, and landmark assets. The earlier `3 implemented / 7 withheld` creature state, `2 promoted / 8 withheld` plant state, and plant promotion timeout are historical orchestration states—not current product blockers.

What remains is authority-gated execution:

- `W1-001-SR` exists as a fully enumerated structured intake row and is approval-ready as written: eighteen aliases, three narrative-only terms, and one new item (`aionbound:wing_bone_stay`).
- `W1-003-STORM-NEST` exists but is not execution-ready. Boss, arena, phases, and attacks are bound; thresholds, timing, reset, multiplayer, restart, persistence, and terminal semantics are absent.
- `W1-004-SR` exists but is not execution-ready. Loot identities and `aionbound:storm_pinion` are bound; numeric envelopes, chest rolls, guaranteed apex package, recovery, repeat-clear, and mastery semantics are absent.

Until those tranches are ratified, the 50-row Codex scaffold stays dormant and Skyreach loot, recipes, acquisition, Storm Nest, seal recovery, and terminal progression cannot be called complete.

## Twinbond

Existing authority closes the exact dual wyrm IDs/art, arena/approach inputs, `twinbond_relic`, and Trophy Edge ignition. It does not close:

- isolated container versus same-world placement;
- Concord Spark and Memory of Four Lands presentation;
- any unbound mastery sigil;
- whether Twinbond is mandatory for the Wave 1 machine exit;
- numeric phases, reset, ownership, persistence, terminal reward, recovery, and repeat-clear semantics.

The inherited implementation is not a safe final substitute: it consumes `finale_ignition_key` and awards `trophy_concord_scale`, while current Wave 1 authority binds `twinbond_relic` and Trophy Edge ignition. Twinbond therefore remains final-blocking, not a silent safe defer.

## Technical debt disposition

- Ashen activation is final-blocking but needs no new Creative decision. Its exact services, target handlers, persistence surfaces, and acceptance tests already exist in the deferred activation ticket.
- The literal archive-extracted entrypoint harness is implemented. It becomes final proof only when run against the exact frozen Wave 1 `.mcaddon` with its hash and captured output.
- Stale G6/pre-G6 tests are already quarantined as `.disabled`; they are not a current blocker.
- The evidence-derived receipt mechanism exists. Final qualification receipts must be generated from captured validator/BDS observations; declarative or stale intermediate PASS documents cannot substitute for them.
- Physical console, controller, multiplayer, split-screen, Realms, Marketplace, and release proof remain optional for the engineering `.mcaddon` and forbidden as claims without their own gates.

## Smallest user decision set

1. Approve `W1-001-SR` exactly as mapped, or explicitly revise the Skyreach acquisition contract.
2. Approve a refined `W1-003-STORM-NEST` containing every absent encounter field.
3. Approve a refined `W1-004-SR` containing every absent loot/recovery field.
4. Bind Twinbond container, secondary presentation, machine-exit dependency, encounter envelope, and terminal reward semantics under `W1-CREATIVE-002/003/004`.
5. Provide original shipping audio or explicitly reduce the final sound-identity requirement.

No further human design decision is required for the already-authorized Ashen composition or the final mechanical/package qualification. No `W1-CREATIVE-005` decision is required if its exact defer conditions remain true.

The JSON twin contains exact source functions, hashes, classifications, and minimum acceptance criteria.
