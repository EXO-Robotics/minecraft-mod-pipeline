# Whisperwood Thorn Court Reward Bridge

Status: **TARGETED SOURCE SEMANTICS PASS**

The shipped runtime now consumes the protected Thorn Court material and arena-cache identities without command-based loot. Each eligible participant receives an independently rolled material package through inventory-first delivery with participant-local overflow. The existing `thorn_court.js` service remains the sole owner of once/recovery physical `aionbound:thorn_stalker_skull` fulfillment.

The Ancient Totem barrel remains at exact local offset `(-2, 0, +2)` from its lodestone. Its deterministic structure bytes contain no LootTable NBT. Runtime resolution supports all four horizontal template rotations, cancels pre-clear interaction synchronously, and fills the cache only after a valid arena terminal event. Repeat clears may refill allowlisted cache materials; unknown player storage causes a fail-closed refusal instead of deletion.

This receipt proves source semantics and deterministic structure bytes only. It does not claim BDS load, client UI behavior, packaged execution, same-world restart, or candidate qualification.
