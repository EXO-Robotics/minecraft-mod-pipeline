# Whisperwood direct-prop worldgen lane

Run the deterministic author in write and check modes, followed by the bounded unit tests. This lane owns only the two direct-prop feature and feature-rule registrations and its static evidence.

The validator runs those checks and writes or checks the deterministic validation report.
