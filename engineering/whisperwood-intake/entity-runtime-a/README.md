# Whisperwood Entity Runtime A

This isolated lane binds only `lantern_hare`, `mosskip_fawn`, `mosskip_doe`, `mosskip_buck`, and `rootback_boar` from their exact native-PASS evidence into source-tree BP/RP runtime files.

Regenerate and validate with:

```sh
python3 engineering/whisperwood-intake/entity-runtime-a/build_entity_runtime_a.py
python3 engineering/whisperwood-intake/entity-runtime-a/build_entity_runtime_a_report.py
```

The build fails closed if a native receipt is not `PASS`. Loot is deliberately absent pending W1-CREATIVE-001 and W1-CREATIVE-004. No candidate or runtime claim is made here.
