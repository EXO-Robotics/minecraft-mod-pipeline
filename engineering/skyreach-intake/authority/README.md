# Skyreach authority intake

Run `python3 build_skyreach_authority_map.py` and `python3 -m pytest -q test_skyreach_authority_map.py`.

This lane is authority-only. It owns no behavior/resource-pack files, runtime scripts, build outputs, or qualification claims.
