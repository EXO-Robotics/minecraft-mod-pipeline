{
  "schemaVersion": 1,
  "id": "representative",
  "version": "1.0.0",
  "name": "Representative Reconstruction Fixture",
  "description": "Deterministic source fixture covering direct, scripted, reconstructed, approximate, and unsupported conversion paths.",
  "environment": "*",
  "entrypoints": {
    "main": ["fixture.representative.RepresentativeMod"]
  },
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": "1.20.x"
  },
  "custom": {
    "mccompilerFixture": true,
    "expectedNamespace": "representative"
  }
}
