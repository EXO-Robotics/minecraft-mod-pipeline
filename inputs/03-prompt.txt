You are the single bounded native repair producer for immutable Aionbound Core Beta G5. Work only in this isolated repository, do not use Git, and modify only pre-created paths declared by inputs/01-assignment.json. Read inputs/02-contract.json and inputs/04-repair.json. Do not access outside paths or use Qwen/local models. Preserve every G5 system, selected asset byte, identifier, UUID, stable @minecraft/server 2.0.0 dependency, world pack_scope, no-experiment policy, and G5 archive historically. Produce one replacement generation G6; do not claim BDS or manual gameplay proof.

Repair exactly the consolidated findings:
1. Pocket: deterministically generate a safe <=192-edit cell with a solid floor, a two-block-high air cavity, and an internal aionbound:orevein_hollow_gate. External gate use creates/enters; internal gate use exits to the stored return dimension/location. Persist base, cursor, return authority, owner, and state. On restart rebuild the deterministic edit list and resume one interrupted job from its cursor; never strand the owner in solid blocks. First creation consumes exactly one key only after admission; later entry/exit consumes none. Reject a second owner cell.
2. Combat: give Glasswing, Royal Moth, Chrono Robo, Basalt, Rift, Ash, and Tide stable bounded nearest-player target acquisition plus hurt-by-target and their existing melee attacks. Keep passive entities passive.
3. Encounters: add version-2 world encounter journals. Persist active admission before spawn, reject duplicate active or terminal logical keys, roll back admission if spawn fails, reconcile duplicate live entities, terminal entities, and missing active records on restart, and preserve exact-once rewards. Require glasswing:first_defeat before foundry robot admission. Twinbond remains exactly one Ash plus one Tide under the global cap.
4. Mount: retain minecraft:rideable and minecraft:input_ground_controlled, remove the incompatible redundant minecraft:behavior.controlled_by_player, add explicit ordinary mount interaction text and bounded step handling, and preserve one owner mount/12 world cap.
5. Codex: guidance must expose the Glasswing -> foundry -> ray/pocket/pilgrimage -> bosses -> Edge -> Twinbond endpoint route without adding UI or commands.

Advance BP/RP/module versions to [1,1,1], change the packaged marker to [Aionbound Core Beta] runtime-ready-g6 exactly once immediately before startRuntime(), and output deterministic aionbound-core-beta-g6 behavior/resource/mcaddon archives. Update build, validator, focused tests, source ledger, implementation map, candidate/artifact manifests, and reports. Tests must structurally prove the two-way pocket path, deterministic restart-resume representation, combat targeting on every named combat entity, active encounter dedupe/journal, direct-input mount configuration, foundry prerequisite, stable API/no experiments, exact package membership, packaged literal main entry, complete ledger, and two-build equality. Correct concrete defects inside this activation. Stop after one locally validated immutable G6 candidate.
